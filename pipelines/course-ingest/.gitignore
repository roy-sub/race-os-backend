# Build artefacts. Bundles and terrain extracts belong in object storage behind a
# CDN (RaceOS_Build_Spec.md Part 10.3), and a terrain extract is tens of MB.
# `course-ingest regenerate-all` rebuilds everything from specs/.
out/
.cache/
__pycache__/
*.egg-info/
.pytest_cache/
